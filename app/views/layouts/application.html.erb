<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Movie Review" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest if needed %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Bootstrap for styling %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    
    <%# JavaScript Import Map %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%# Navigation Bar %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="<%= root_path %>">Movie Review</a>
        <div class="ms-auto">
          <%= link_to "All Movies", movies_path, class: "btn btn-outline-light me-2" %>
        </div>
      </div>
    </nav>

    <%# Flash Messages %>
    <div class="container mt-3">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == "notice" ? "success" : "danger" %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
    </div>

    <%# Main Page Content %>
    <div class="container mt-4">
      <%= yield %>
    </div>

    <%# Footer %>
    <footer class="text-center mt-5 p-3 bg-light">
      <p>© <%= Time.current.year %> Movie Review App</p>
    </footer>

    <%# Bootstrap JavaScript %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
